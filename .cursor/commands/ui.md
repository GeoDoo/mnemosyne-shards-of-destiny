Refactor/Create this UI using Phaser Best Practices:
- Use relative positioning (`width * 0.5`) and anchors.
- Ensure `scaleMode: Phaser.Scale.FIT` logic is considered.
- Implement large, touch-friendly hit areas for buttons.
- Use a dedicated `UIScene` that runs in parallel to the game.
