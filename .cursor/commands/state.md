Convert the logic in this file into a Finite State Machine (FSM).
- Create a clear `states` object (e.g., IDLE, RUN, ATTACK).
- Ensure transitions are handled via events, not constant polling in `update()`.
- Keep the Scene's `update()` method as empty as possible.
